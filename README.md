# MixSplitR
What it does:  Detects if your file is a single track or a mix Splits mixes into individual tracks (using silence detection) Identifies each song via audio fingerprinting (ACRCloud) Tags everything with metadata + album art Organizes into artist folders as FLAC files
