# MixSplitR v8.0 - Python Dependencies (Full Feature Parity)
# Install with: pip install -r requirements.txt

# === Core (Required) ===
PySide6>=6.5
pydub>=0.25.1
requests>=2.28
mutagen>=1.47
tqdm>=4.64

# === Music Identification ===
# ACRCloud: On Windows with Python 3.11+, install manually:
#   pip install acrcloud-sdk-python
# Falls back gracefully if unavailable.
musicbrainzngs>=0.7
pyacoustid>=1.3.0
shazamio>=0.6.0
discid>=1.3.0

# Async deps (used by shazamio, explicit for PyInstaller bundling)
aiohttp>=3.8
aiosignal>=1.4.0
frozenlist>=1.1.1
multidict>=4.5
yarl>=1.17.0
async_timeout>=4.0
attrs>=17.3.0
charset_normalizer>=2

# === Audio Analysis & Recording ===
librosa>=0.10
numpy>=1.23
scipy>=1.10
numba>=0.51
soundfile>=0.12
soundcard>=0.4
sounddevice>=0.4

# === Interactive CLI UI ===
prompt_toolkit>=3.0
wcwidth>=0.2

# === System Utilities ===
psutil>=5.9
certifi>=2024.0

# === Build (only needed for compiling .exe) ===
# pyinstaller>=6.0
